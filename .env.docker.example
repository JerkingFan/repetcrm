# Локальные переменные для docker compose (dev). Скопируйте: cp .env.docker.example .env.docker
# НЕ используйте в production — для prod см. .env.production.example

SECRET_KEY=repetcrm-local-dev-only-not-for-production-min-32-chars
APP_ENV=development
